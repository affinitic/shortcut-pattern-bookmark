import {
  useGetCurrentTabUrl,
  onClickShortcut,
  setStorageList,
  getStorageList,
  useLocalStorage,
} from "./chrome";
import { generateId } from "./string";
import { newUrlGeneration, extractPartOfUrl } from "./url";

export {
  useGetCurrentTabUrl,
  onClickShortcut,
  setStorageList,
  getStorageList,
  useLocalStorage,
  generateId,
  newUrlGeneration,
  extractPartOfUrl,
};
