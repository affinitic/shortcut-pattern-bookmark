import {
  useGetCurrentTabUrl,
  onClickShortcut,
  setStorageList,
  useLocalStorage,
} from "./chrome";
import { generateId } from "./string";
import { newUrlGeneration, extractPartOfUrl } from "./url";

export {
  useGetCurrentTabUrl,
  onClickShortcut,
  setStorageList,
  useLocalStorage,
  generateId,
  newUrlGeneration,
  extractPartOfUrl,
};
