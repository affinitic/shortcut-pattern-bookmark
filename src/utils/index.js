import {
  useGetCurrentTabUrl,
  onClickShortcut,
  setStorageList,
  getStorageList,
  useLocalStorage,
} from "./chrome";
import { stringToId } from "./string";
import { newUrlGeneration, extractPartOfUrl } from "./url";

export {
  useGetCurrentTabUrl,
  onClickShortcut,
  setStorageList,
  getStorageList,
  useLocalStorage,
  stringToId,
  newUrlGeneration,
  extractPartOfUrl,
};
